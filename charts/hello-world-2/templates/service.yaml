apiVersion: v1
kind: Service
metadata:
  name: {{ include "hello-world-2.fullname" . }}
  labels:
    {{- include "hello-world-2.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hello-world-2.selectorLabels" . | nindent 4 }}
